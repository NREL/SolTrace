---
title: "Ray Counts"
---

The ray count controls how many rays the trace attempts to launch. More rays reduce statistical noise but take longer to run.

The maximum ray count limits how many traced ray records are retained for analysis. CPU cores controls how many worker threads the CPU runners may use. The seed makes repeated traces reproducible.
