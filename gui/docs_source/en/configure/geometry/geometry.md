---
title: "Geometry"
---

Geometry entries define reusable surface shapes. A geometry can be assigned to one or more elements in the layout, which keeps repeated shapes consistent across the scene.

Edit the parameters for the selected shape type, then assign that geometry from the Layout panel.

For example, a circular aperture area is:

$$
A = pi r^2
$$
