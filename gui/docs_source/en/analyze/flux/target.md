---
title: "Flux Map Target"
---

Select an element from the list or use the pick tool to choose one directly from the 3D result view. Compute Map projects ray interactions onto that element's surface mesh, accumulates DNI-scaled ray power on the surface, and renders the result as a texture on the element.
